# Python for BioImage Analysis

## Dev

- Use pre-commit to ensure that the exercises are correctly formatted and up to date.
- To maintain both ways synchronization between notebooks and scripts, add the `jupytext`
VSCode extension.
- Option in `pyproject.toml` syncs notebook to script when saving in Jupyter.